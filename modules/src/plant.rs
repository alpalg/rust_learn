pub mod plant {
    pub struct Vegetable {
        pub name: String,
        id: u16,
    }

    impl Vegetable {
        pub fn new(name: &str) -> Vegetable {
            Vegetable {
                name: String::from(name),
                id: 1,
            }
        }
    }
}